# 🗺️ แผนที่ระบบทั้งหมด (Systems Map)

> ไฟล์อ้างอิงกลางสำหรับทีม AI ทุกตัว โดยเฉพาะ `coder` และ `data`
> **กฎเหล็ก: ก่อนแตะโค้ดทุกครั้ง ต้องรู้ก่อนว่างานนี้เป็นของระบบไหน** — ถ้าใบงานไม่ระบุ ให้ถามกลับก่อน อย่าเดา
> เพราะแต่ละระบบเป็นคนละไฟล์ คนละโปรเจกต์ GAS คนละ deployment — แก้ผิดตัว = พังของจริงที่ทีมใช้อยู่

## ระบบทั้งหมดของคุณปาล์ม

| # | ระบบ | ธุรกิจ | โค้ดอยู่ที่ | หลังบ้าน | หมายเหตุ |
|---|---|---|---|---|---|
| 1 | **แอปสั่งน้ำละกอน** (ลูกค้าสั่งน้ำ) | 💧 โรงน้ำ | `index.html` (root ของ repo) | GAS โปรเจกต์ของตัวเอง (`GAS_URL` ในไฟล์) + LIFF SDK | ธีมน้ำเงิน-ทอง ฟอนต์ Sarabun |
| 2 | **แอปร้าน Old Days** (หลังร้านคาเฟ่: ปิดยอด/สต๊อก/เบิกซื้อ/เมนู/ค่าคอม) | ☕ คาเฟ่ | `olddays/index.html` | `olddays/gas/Code.gs` — GAS **คนละโปรเจกต์** กับข้อ 1 | LIFF `2011084984-YNPjVDiA`, หน้าเว็บเสิร์ฟผ่าน GitHub Pages จาก branch `main`, ธีมน้ำตาลวินเทจ |
| 3 | **คุณเลขา** (บอท LINE + บอร์ดงาน + คลังข้อมูล) | 🏢 กลาง (ทั้งสองธุรกิจ) | `secretary/Code.gs` | GAS **คนละโปรเจกต์** อีกตัว | webhook LINE OA, ห้ามสับสนกับ `olddays/gas/Code.gs` — ชื่อไฟล์เหมือนกันแต่คนละระบบ! |
| 4 | **แอปทีมเซลล์** (ละกอน Sales) | 💧 โรงน้ำ | `plant-systems/sales/` | GAS โปรเจกต์ **Sale System** | ลูกค้า/ใบเสนอราคา/เยี่ยม/รอบวิ่ง-รถแวน/คู่แข่ง |
| 5 | **แอปโรงงาน + คลังสินค้า** (ละกอน โรงงาน) | 💧 โรงน้ำ | `plant-systems/factory/` | GAS โปรเจกต์ **Lakon Factory** | ออเดอร์ทุกช่องทาง/ผลิต/รอบผลิต/สต๊อกสินค้า+วัตถุดิบ(BOM)/จัดส่ง/พนักงาน — "คลังสินค้า" อยู่ในแอปนี้ ไม่ใช่โปรเจกต์แยก |
| 6 | **แอปผู้บริหาร** (ORIGIN LABS) | 🏢 กลาง | `exec/index.html` | ใช้ GAS ของคุณเลขา | อ่านชีตระบบขาย-โรงงานแบบอ่านอย่างเดียว (`PLANT_SHEET_ID`) + แผนธุรกิจในชีตเลขา |

> ⚠️ ระบบ 4 กับ 5 **ใช้สเปรดชีตเดียวกัน** — แก้โครงสร้างชีตฝั่งหนึ่งกระทบอีกฝั่งเสมอ
> โค้ดในโฟลเดอร์ `plant-systems/` เป็น **สำเนา** ของจริงรันอยู่ใน GAS — แก้แล้วต้องก็อปกลับไปวางใน Apps Script
> `SPREADSHEET_ID` ถูกถอดออกจากสำเนา (repo สาธารณะ) ของจริงอ่านจาก Script Properties

## ⚠️ กันพลาดยอดฮิต

1. **`Code.gs` มี 2 ไฟล์คนละระบบ** — `secretary/Code.gs` (เลขา) กับ `olddays/gas/Code.gs` (คาเฟ่) เช็ค path ให้ชัวร์ก่อนแก้ทุกครั้ง
2. **Deploy แยกกันหมด** — แก้โค้ด GAS ระบบไหน ต้อง deploy ที่โปรเจกต์ GAS ของระบบนั้น และใช้วิธี **Manage deployments → ✏️ → New version เท่านั้น** (ห้าม "New deployment" — URL จะเปลี่ยนแล้วท่อพัง: LINE webhook, Routine, ลิงก์บอร์ด)
3. **หน้าเว็บ Old Days อัปเดตผ่าน `main` เท่านั้น** — โค้ดที่ยังไม่ merge เข้า `main` จะไม่ปรากฏในแอปจริง
4. **คำในใบงานอาจกำกวม** — "แอป" คำเดียวอาจหมายถึงระบบไหนก็ได้ ให้ดูคำใบ้: ธุรกิจ (โรงน้ำ/คาเฟ่), ฟีเจอร์ที่พูดถึง (สต๊อก/ปิดยอด=Old Days, สั่งน้ำ=ละกอน, ประกาศ/บอร์ด=เลขา, ออเดอร์ลูกค้า/OEM=ระบบขาย) — ไม่ชัวร์ = ถาม

## เอกสารเพื่อนบ้าน

- ตัวตนแบรนด์ละกอน: `docs/ai-team/brand-lakon.md`
- คู่มือเลขา LINE: `docs/ai-team/secretary-line-bot.md`
- บอร์ดงาน: `docs/ai-team/job-board.md`
- ประวัติ/บริบทที่นำเข้าจากภายนอก (เช่น สรุปงานจาก ChatGPT): `docs/ai-team/history/`
