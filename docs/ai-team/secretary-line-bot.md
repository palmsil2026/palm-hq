# 🤖 คุณเลขา — บอท LINE AI (คู่มือติดตั้ง)

"คุณเลขา" คือผู้ช่วย AI ของคุณปาล์ม ที่ทีมงานทักผ่าน LINE ได้เหมือนแชทกับเลขาจริง
บุคลิกสาย **Donna Paulsen × Pepper Potts** — เก่ง มั่นใจ อบอุ่นแต่เด็ดขาด

```
พนักงานทัก LINE OA
      │
      ▼
  GAS Webhook (secretary/Code.gs)
      │
      ├─ เรื่องทั่วไป/ฝากงาน → ให้ Claude (AI) ตอบด้วยบุคลิกคุณเลขา
      └─ เรื่องเงิน/ต้นทุน    → ปฏิเสธสุภาพ + 🔔 เด้งเตือนคุณปาล์มทันที
      ▼
  ตอบกลับเข้า LINE
```

ไฟล์ที่เกี่ยวข้อง:
- `secretary/Code.gs` — โค้ด backend (วางใน Google Apps Script)
- `secretary/system-prompt.md` — บุคลิก/กฎของคุณเลขา (ต้นฉบับอ่านง่าย)

---

## 🧩 สิ่งที่ต้องเตรียม

| ต้องมี | ได้จากไหน | ค่าใช้จ่าย |
|---|---|---|
| **LINE Official Account + Messaging API** | developers.line.biz | ฟรี |
| **Claude API key** | console.anthropic.com | จ่ายตามใช้ (~฿0.12/ข้อความ ด้วย Haiku) |
| **LINE userId ของคุณปาล์ม** | ดูวิธีด้านล่าง | ฟรี |
| Google Sheet (ไม่บังคับ) | เก็บ log บทสนทนา | ฟรี |

---

## ⚙️ ขั้นตอนติดตั้ง

### 1) สร้าง LINE Official Account + Messaging API
1. เข้า **developers.line.biz** → สร้าง Provider → สร้าง **Messaging API channel**
2. ไปแท็บ **Messaging API** → กด **Issue** เพื่อสร้าง **Channel access token** → คัดลอกเก็บไว้
3. เปิด **Use webhook** = ON, ปิด **Auto-reply / Greeting messages** (กันบอทซ้ำ)

### 2) สร้าง Claude API key
1. เข้า **console.anthropic.com** → **Billing** เติมเครดิต (เริ่ม ~$5 พอ)
2. **API Keys** → **Create Key** → คัดลอกเก็บไว้ (ขึ้นครั้งเดียว!)

### 3) วางโค้ดใน Google Apps Script
1. เปิด **script.google.com** → New project
2. วางเนื้อหาจาก `secretary/Code.gs` ลงไป
3. **Project Settings** (เฟือง) → เลื่อนหา **Script properties** → **Add** ทีละค่า:

| Property | ค่า |
|---|---|
| `LINE_TOKEN` | Channel access token จากขั้นที่ 1 |
| `ANTHROPIC_API_KEY` | API key จากขั้นที่ 2 |
| `OWNER_LINE_USER_ID` | LINE userId ของคุณปาล์ม (ขั้นที่ 5) |
| `LOG_SHEET_ID` | (แนะนำ) Google Sheet — ใช้เก็บ log + ความจำ + บอร์ดงาน |
| `REQUEST_SHEET_ID` | (ไม่บังคับ) ถ้าอยากแยกชีตบอร์ดงานคนละไฟล์ ไม่ใส่ = ใช้ `LOG_SHEET_ID` |

### 4) Deploy เป็น Web app
1. **Deploy → New deployment → Web app**
2. Execute as: **Me** | Who has access: **Anyone**
3. คัดลอก **Web app URL**
4. เอา URL ไปวางใน LINE Developers → **Messaging API → Webhook URL** → กด **Verify**

### 5) หา LINE userId ของคุณปาล์ม (ไว้เด้งเตือน)
- ให้คุณปาล์มแอดบอทเป็นเพื่อน แล้วทักอะไรก็ได้ 1 ข้อความ
- ดู log ใน Apps Script (**Executions**) หรือเปิด `LOG_SHEET_ID` ไว้ → จะเห็น userId ในคอลัมน์ "ผู้ส่ง"
- คัดลอก userId นั้นไปใส่ใน `OWNER_LINE_USER_ID` แล้ว re-deploy

### 6) ทดสอบ
- ในตัว editor ของ Apps Script เลือกฟังก์ชัน **`testClaude`** แล้วกด **Run** → ดูว่า Claude ตอบกลับใน Log
- จากนั้นทัก LINE OA จริง → คุณเลขาควรตอบกลับ ✅
- ลองพิมพ์คำว่า "ต้นทุนน้ำขวดละเท่าไหร่" → ต้องโดนปฏิเสธ + คุณปาล์มได้ข้อความเด้งเตือน

---

## 🎚️ ปรับแต่งได้

- **เปลี่ยนรุ่น AI:** แก้ตัวแปร `MODEL` ใน `Code.gs`
  - `claude-haiku-4-5` = ถูก+เร็ว (แนะนำเริ่มต้น)
  - `claude-sonnet-5` = ฉลาดกว่า ตอบซับซ้อนดีขึ้น (แพงกว่า)
- **ปรับบุคลิก/คำพูด:** แก้ `SYSTEM_PROMPT` ใน `Code.gs` (ต้นฉบับอยู่ใน `system-prompt.md`)
- **เพิ่มคำที่ห้ามพูด:** เพิ่มคำในลิสต์ `FINANCE_KEYWORDS`
- **เปลี่ยนวิธีเด้งเตือน:** แก้ฟังก์ชัน `alertOwner`

---

## 🔐 ความปลอดภัย

- **API key = รหัสบัตรเครดิต** เก็บใน Script Properties เท่านั้น ห้าม hardcode ในโค้ด ห้าม commit ขึ้น GitHub
- เรื่องการเงินถูกกันไว้ 2 ชั้น: ตัวกรองคำ (`FINANCE_KEYWORDS`) + คำสั่งในบุคลิก (`SYSTEM_PROMPT`)
- ถ้ากังวลเรื่องคนแปลกหน้าแอด: จำกัดว่าใครแอด OA ได้ หรือเพิ่มการเช็ค userId (allowlist) ในภายหลัง

---

## 🧠📋 ความจำ + บันทึกงาน (เปิดใช้แล้ว)

โค้ดเวอร์ชันนี้ทำ 2 อย่างนี้ให้อัตโนมัติ **ถ้ามีการตั้ง `LOG_SHEET_ID`** (หรือ `REQUEST_SHEET_ID`):

- 🧠 **ความจำบทสนทนา** — จำ 10 ข้อความล่าสุดต่อคน เก็บในแท็บ **`Memory`** → คุยต่อเนื่องได้ (เลขาจำว่าคุยอะไรค้างไว้)
- 📋 **บันทึกงานลงบอร์ด** — พอมีคนฝากงาน คุณเลขาจะลงแท็บ **`Requests`** (ชีตเดียวกับฟอร์ม `request.html`) พร้อมออกเลขงาน `#REQ...` ให้อัตโนมัติ

> แท็บ `Memory` และ `Requests` จะถูกสร้างเองอัตโนมัติในไฟล์ Sheet ที่ตั้งไว้

**ทดสอบ:** พิมพ์ *"ฝากสั่งน้ำ 20 แพ็คส่งร้านกาแฟพรุ่งนี้"* → คุณเลขาควรรับเรื่อง + ขึ้น "บันทึกเป็นงาน #REQ... แล้วค่ะ" และมีแถวใหม่ในแท็บ `Requests`

---

## 📊📣📚 อ่านบอร์ด + เด้งเตือน + คลังข้อมูล (เปิดใช้แล้ว)

- 📊 **เลขาอ่าน/สรุปบอร์ดได้** — ถามได้เลย เช่น *"มีงานค้างอะไรบ้าง"*, *"สรุปงานวันนี้"*, *"คิวงานโรงน้ำ"*
  - 🔒 **คุณปาล์ม (เจ้าของ)** ถาม → เห็นงานทั้งทีม
  - 👤 **พนักงาน** ถาม → เห็นเฉพาะงานที่ตัวเองฝาก (กันความลับ)
  - _เลขารู้ว่าใครเป็นเจ้าของจากค่า `OWNER_LINE_USER_ID`_
- 📣 **ส่งต่อถึงคุณอัตโนมัติ** — เจอข้อเสนอที่ต้องให้คุณตัดสินใจ (ขอลดราคา/ดีล/ข้อเสนอ) หรือการทวงงาน/ตามงาน → เลขาสรุปแล้ว push หาคุณ
- 🔴 **งานด่วนมาก** — พอมีคนฝากงานที่ระดับ "ด่วนมาก" → เด้งเตือนคุณทันที (คุณไม่ต้องเปิดบอร์ดเองตลอด)
- 📚 **คลังข้อมูลธุรกิจ** — เลขาอ่านแท็บ **`ข้อมูลโรงงาน`** เป็นความรู้พื้นฐานทุกครั้งที่ตอบ

### วิธีสอนข้อมูลให้คุณเลขา
เปิด Google Sheet → แท็บ **`ข้อมูลโรงงาน`** (สร้างอัตโนมัติ) → กรอกเป็นแถว **หัวข้อ | รายละเอียด** เช่น:

| หัวข้อ | รายละเอียด |
|---|---|
| เวลาทำการ | จ-ส 8:00-17:00 |
| พื้นที่ส่งน้ำ | ในเขตอำเภอเมือง + รอบนอก 10 กม. |
| ขนาดบรรจุ | ถัง 20 ลิตร, แพ็คขวด 600ml x12 |
| โปรโมชั่น | สั่ง 10 แถม 1 |
| เมนูคาเฟ่ขายดี | ลาเต้, อเมริกาโน่, มัทฉะ |

> ⚠️ **ใส่เฉพาะข้อมูลที่พนักงาน/ลูกค้ารู้ได้** — ห้ามใส่ต้นทุน/กำไร/ยอดขาย (เลขายังถูกกันไม่ให้เปิดเผยเรื่องเงินอยู่)

---

## 🗂️ คัดแยกงาน + จ่ายงานให้ AI (เปิดใช้แล้ว)

เมื่อมีคนฝากงาน คุณเลขาจะ **คัดแยกเป็น 3 กอง** อัตโนมัติ (ช่อง `ผู้รับผิดชอบ` ในบอร์ด):

| กอง | เลขาทำอะไร | ในบอร์ด |
|---|---|---|
| 🟢 **เลขาทำเอง** (งานเบา: ร่างข้อความ/แคปชั่น, สรุปสั้น, คิดเลขทั่วไป) | ทำให้เลยในแชท | ไม่ลงบอร์ด (เสร็จแล้ว) |
| 🤖 **ทีม AI** (งานหนัก: วิเคราะห์ลึก, สไลด์, โค้ด, ค้นข้อมูล) | จดเข้าคิว สถานะ **"รอทีม AI"** | ผู้รับผิดชอบ = ทีมAI |
| 👤 **คน** (ส่งของ/ซ่อม/ซื้อของ) | จดลงบอร์ด สถานะ "ใหม่" | ผู้รับผิดชอบ = คน |

### วิธีให้ทีม AI เคลียร์คิว (ตอนนี้ = ทำในห้อง Claude Code)
งานที่สถานะ **"รอทีม AI"** ในบอร์ด = คิวรอทีม 7 แผนกทำ
- เปิด Claude Code (ห้องนี้) แล้วสั่งว่า *"เคลียร์คิวงานทีม AI ในบอร์ด"* → ทีมจะหยิบไปทำด้วยเครื่องมือเต็ม
- (อนาคต: ตั้ง trigger ให้เปิดเองตามรอบ — ดูหัวข้อต่อยอด)

---

## 👥 คุณเลขาในกลุ่มไลน์ (เปิดใช้แล้ว)

ดึงคุณเลขาเข้ากลุ่มได้ (กลุ่มคาเฟ่, กลุ่มทีมเซลล์ ฯลฯ) โดยเธอจะ **ทำตัวเป็นธรรมชาติเหมือนสมาชิกคนหนึ่ง**:

- 🤫 **เงียบเป็นปกติ** — ในกลุ่ม เลขาจะ **ไม่ตอบทุกข้อความ** ปล่อยให้คนคุย/แจ้งงานกันเอง
- 🗣️ **พูดเฉพาะตอนถูกเรียก** — ตอบเมื่อข้อความมีคำว่า **"เลขา"** (หรือ @mention เธอ) เท่านั้น
- 📢 **ส่งรายงาน/แจ้งเตือนเข้ากลุ่มได้** — เช่น สรุปประจำวันเข้ากลุ่มคาเฟ่

### วิธีเปิดใช้งานในกลุ่ม
1. **อนุญาตให้บอทเข้ากลุ่ม:** ที่ **manager.line.biz → Settings → Response settings** เปิด **"Allow joining group chats / อนุญาตให้เข้ากลุ่ม"**
2. เชิญ OA คุณเลขาเข้ากลุ่ม
3. **หา group id** (ไว้ตั้งค่าส่งรายงาน): พิมพ์ในกลุ่มว่า **`เลขา group id`** → เลขาจะตอบ chat id ของกลุ่มนั้นมาให้
4. เอา group id ไปตั้งใน **Script properties** เช่น:
   - `GROUP_CAFE_ID` = id กลุ่มคาเฟ่
   - `GROUP_SALES_ID` = id กลุ่มทีมเซลล์
5. ทดสอบส่ง: รันฟังก์ชัน **`testPushCafe`** ใน editor → เลขาจะทักเข้ากลุ่มคาเฟ่

> 💡 พอตั้ง group id ไว้แล้ว ระบบสรุปประจำวัน (เฟสถัดไป) จะส่งเข้ากลุ่มที่ถูกต้องได้อัตโนมัติ

---

## 🗺️ อยากต่อยอดอีก (เฟสถัดไป)

## ⏰ งานอัตโนมัติตามเวลา (เปิดใช้แล้ว)

ตั้งใน GAS (ทำได้ละเอียดถึงทุก 30 นาที — ละเอียดกว่าฝั่ง Claude Code):

| ฟังก์ชัน | เมื่อไหร่ | ทำอะไร |
|---|---|---|
| `morningBrief` | ทุกวัน **8 โมงเช้า** | สรุปงานค้าง/ด่วน push หาคุณปาล์ม |
| `boardWatch` | **ทุก 30 นาที** | เจองานด่วน "ใหม่" เข้าบอร์ด → เด้งเตือนทันที (กันสแปม เตือนเฉพาะงานด่วน) |

### วิธีเปิด (ทำครั้งเดียว)
1. **ตั้ง Time zone ให้ถูก** — Project Settings → **Time zone** → เลือก **(GMT+07:00) Bangkok** (ไม่งั้น 8 โมงจะเพี้ยน)
2. เปิด editor → เลือกฟังก์ชัน **`setupTriggers`** → กด **Run** (ครั้งแรกจะขอ authorize ให้กดอนุญาต)
3. เช็คว่าติดตั้งแล้วที่เมนู **Triggers** (⏰ ด้านซ้าย) — ควรเห็น 2 ตัว
4. ทดสอบเลย: รัน **`morningBrief`** เอง → คุณควรได้ข้อความสรุปเข้า LINE

> 🔧 อยากเปลี่ยนเวลา/ความถี่ แก้ใน `setupTriggers()` แล้วรันใหม่ (`atHour(8)`, `everyMinutes(30)` — ค่าที่ใช้ได้: 1,5,10,15,30)

> ⚠️ **หมายเหตุ:** `boardWatch` **เตือน**งานด่วนให้ แต่ยัง**ไม่ได้สั่งทีม AI ลงมือทำ** — งานหนักที่รอทีม AI ยังต้องเปิด Claude Code สั่ง "เคลียร์คิว" (หรือทำระบบ C ในอนาคต)

---

### 📌 พัก/จดไว้: AI สรุปประจำวันเข้ากลุ่ม (รอลงรายละเอียด)
คอนเซ็ปต์ที่คุยไว้ (ยังไม่ทำ):
- 📊 **รายงานขายคาเฟ่ประจำวัน** → โพสต์เข้า **กลุ่มไลน์ร้านคาเฟ่**
- 💧 **ยอดขายขวด/ทีมเซลล์** → โพสต์เข้า **กลุ่มไลน์ทีมเซลล์**
- ⏰ เวลาประมาณ **16:50**
- ใช้ **AI ตัวเดียวโมเดลบาง (Haiku)** ตั้งเวลาเดินเอง
- _(ยังต้องตกลงว่าจะสรุป "อะไรบ้าง" + ต่อแหล่งข้อมูลยอดขาย)_


- ⏰ **สรุปทุกเช้า** — ให้คุณเลขา push สรุปงาน/คิวให้คุณปาล์มทุกเช้า (ใช้ Time-driven trigger)
- 🔗 **เชื่อมทีม 7 แผนก** — ให้เลขาส่งงานบางอย่างต่อให้แผนกที่เกี่ยวข้อง
- 🖼️ **รับรูปภาพ** — ให้เลขารับรูปที่แนบมากับงานได้

บอกได้เลยครับถ้าอยากทำเฟสไหนต่อ 😄
