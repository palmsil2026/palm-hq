# 📋 งานบ้านเรา — ระบบจัดการงานครอบครัว (LIFF)

กระดานงานส่วนกลางของครอบครัว เปิดใช้ในไลน์กลุ่มได้ ทุกคนเห็นงานเดียวกัน
รู้ว่าใครลีดงานไหน เดดไลน์เมื่อไหร่ ด่วนแค่ไหน แยกหมวดตามธุรกิจ
**โรงน้ำ 💧 / โรงขวด 🍼 / ร้านกาแฟ ☕ / อื่นๆ 🏠**

## ฟีเจอร์

- **กระดานงานส่วนกลาง** — ทุกคนในกลุ่มเห็นงานชุดเดียวกัน อัปเดตแบบเรียลไทม์เมื่อเปิดแอป
- **ผู้รับผิดชอบหลัก + ผู้ช่วย** — ระบุคนลีดงาน 1 คน และเลือกคนช่วยได้หลายคน
- **เดดไลน์** — แสดงป้าย "วันนี้ / พรุ่งนี้ / อีก X วัน / ⏰เลยกำหนด X วัน" อัตโนมัติ
- **ความเร่งด่วน 3 ระดับ** — 🔥 ด่วนมาก / ⚡ ด่วน / 🌿 ปกติ (แถบสีข้างการ์ด)
- **สถานะงาน** — รอทำ → 🔨 กำลังทำ → ✅ เสร็จแล้ว (เปิดงานใหม่ได้ถ้าต้องแก้)
- **หมวดหมู่ธุรกิจ** — โรงน้ำ / โรงขวด / ร้านกาแฟ / อื่นๆ พร้อมตัวกรอง
- **บันทึกความคืบหน้า** — พิมพ์อัปเดตสั้นๆ ใต้แต่ละงาน มีประวัติว่าใครทำอะไรเมื่อไหร่
- **สรุปหน้าแรก** — งานค้างกี่งาน เลยกำหนดกี่งาน แต่ละคนถืองานกี่งาน
- **แชร์เข้าไลน์** — ส่งการ์ดงาน (Flex Message) หรือสรุปงานค้างทั้งหมดเข้าห้องแชทได้ทันที

## โครงสร้างระบบ

```
LINE กลุ่มครอบครัว
   └── เปิดลิงก์ LIFF → tasks/index.html (โฮสต์บน GitHub Pages)
          └── เรียก API → Google Apps Script (gas/tasks-backend.gs)
                 └── เก็บข้อมูล → Google Sheet (ชีต Members + Tasks)
```

## วิธีติดตั้ง (ทำครั้งเดียว)

### 1. สร้าง Google Sheet + Apps Script

1. สร้าง Google Sheet เปล่าใหม่ 1 ไฟล์ (เช่นชื่อ "ละกอน-งานบ้านเรา")
2. คัดลอก **Sheet ID** จาก URL: `docs.google.com/spreadsheets/d/<ตรงนี้คือ Sheet ID>/edit`
3. ในชีต เปิดเมนู **Extensions → Apps Script**
4. ลบโค้ดเดิม แล้ววางโค้ดจากไฟล์ [`gas/tasks-backend.gs`](gas/tasks-backend.gs) ทั้งหมด
5. แก้บรรทัด `const SHEET_ID = "..."` ให้เป็น Sheet ID ของคุณ
6. เลือกฟังก์ชัน `setup` แล้วกด **Run** หนึ่งครั้ง (อนุญาตสิทธิ์ตามที่ถาม)
   → จะได้ชีต `Members` และ `Tasks` พร้อมหัวตาราง
7. กด **Deploy → New deployment → Web app**
   - Execute as: **Me**
   - Who has access: **Anyone**
8. คัดลอก **Web app URL** เก็บไว้

### 2. สร้าง LIFF app ใน LINE Developers

1. เข้า [LINE Developers Console](https://developers.line.biz/console/) → เลือก Channel เดิม (ที่ใช้กับระบบสั่งน้ำ) หรือสร้าง LINE Login channel ใหม่
2. แท็บ **LIFF → Add** สร้าง LIFF app ใหม่:
   - Size: **Full**
   - Endpoint URL: `https://palmsil2026.github.io/lakon-liff/tasks/`
   - Scope: ติ๊ก `profile`
   - **เปิดใช้ shareTargetPicker** (จำเป็นสำหรับปุ่มแชร์)
3. คัดลอก **LIFF ID** เก็บไว้

### 3. ใส่ค่าในโค้ด

แก้ไฟล์ `tasks/index.html` ตรงส่วนบนสุด:

```js
const GAS_URL = "https://script.google.com/macros/s/....../exec"; // จากขั้นตอนที่ 1
const LIFF_ID = "xxxxxxxxxx-xxxxxxxx";                            // จากขั้นตอนที่ 2
```

commit + push ขึ้น GitHub แล้วรอ GitHub Pages อัปเดต (1-2 นาที)

### 4. เอาเข้าไลน์กลุ่ม

1. ส่งลิงก์ `https://liff.line.me/<LIFF_ID>` เข้าไลน์กลุ่มครอบครัว แล้ว**ปักหมุด/เก็บใน Keep** ไว้
   (หรือตั้งเป็นเมนู Rich Menu ของ LINE OA ก็ได้)
2. ทุกคนกดลิงก์ครั้งแรก → ตั้งชื่อเล่น → ใช้งานได้เลย
3. เวลาสร้างงานใหม่ ระบบจะชวนแชร์การ์ดงานเข้าห้องแชทให้อัตโนมัติ

> **เคล็ดลับ:** ถ้าเปิดแอปจาก**ในห้องแชทกลุ่มโดยตรง** ปุ่มแชร์จะส่งข้อความเข้าห้องนั้นทันที
> ถ้าเปิดจากที่อื่น จะมีหน้าต่างให้เลือกห้องที่จะส่ง (shareTargetPicker)

## การใช้งานประจำวัน

| อยากทำอะไร | ทำยังไง |
|---|---|
| เพิ่มงานใหม่ | กดปุ่ม **+** มุมขวาล่าง → กรอกงาน เลือกคนลีด เดดไลน์ ความด่วน |
| ดูงานตัวเอง | กดตัวกรอง **🙋 ของฉัน** |
| ดูงานเฉพาะร้านกาแฟ | กดตัวกรอง **☕ ร้านกาแฟ** |
| เริ่มทำ / ปิดงาน | แตะการ์ดงาน → กด **🔨 เริ่มทำ** หรือ **✅ เสร็จแล้ว** |
| อัปเดตความคืบหน้า | ในหน้างาน พิมพ์ในช่อง "บันทึกความคืบหน้า" |
| ทวงงานในกลุ่ม 😄 | หน้าแรกกด **แชร์สรุป 📤** → ส่งรายการงานค้างทั้งหมดเข้ากลุ่ม |

## หมายเหตุ

- ข้อมูลทั้งหมดอยู่ใน Google Sheet ของคุณเอง — เปิดดู/แก้/ทำรายงานเพิ่มได้ตลอด
- ไฟล์ `index.html` เดิม (ระบบสั่งน้ำละกอน 💧) ไม่ถูกแตะต้อง ใช้งานได้เหมือนเดิม
- ถ้าอยากเพิ่ม/เปลี่ยนหมวดธุรกิจ แก้ได้ที่ค่าคงที่ `CATEGORIES` ใน `tasks/index.html`
